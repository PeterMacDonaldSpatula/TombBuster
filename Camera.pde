//not sure if I'll use this or pushmatrix method. I found this class online so we won't use it in the final submission but it helps to see this and get a better understanding of how it works.

class Camera {
  PVector pos; //Camera's position 

  Camera() {
    pos = new PVector(0, 0);
  }

  void draw(float x, float y) {
    
  }
  
  void update(float x, float y){
    
  }
}